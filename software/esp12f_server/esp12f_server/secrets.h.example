#pragma once
// Template for WiFi credentials. Copy this file to `secrets.h` (which is
// gitignored) and fill in your own values. Never commit real secrets.
#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASS "YOUR_PASSWORD"

// Optional: Cloudflare Worker URLs for trimmed feeds (see proxy/).
// Leave undefined to disable (Mars still shows time/sols/season; launch hits LL2 directly).
// #define MARS_PROXY_URL   "https://mars-weather.you.workers.dev"
// #define LAUNCH_PROXY_URL "https://launch.you.workers.dev"
// #define AIR_PROXY_URL    "https://air.you.workers.dev"
